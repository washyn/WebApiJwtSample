<source>
  @type http
  port 8888
  bind 0.0.0.0
  add_http_headers false
</source>

<match **>
  @type copy

  <store>
    @type stdout
  </store>

  <store>
    @type file
    path C:/Logs/fluentd/app

    <buffer time>
      @type file
      path C:/Logs/fluentd/buffer

      timekey 1h
      timekey_wait 5m
      timekey_use_utc false

      flush_mode interval
      flush_interval 20s
    </buffer>

    <format>
      @type json
    </format>
  </store>

</match>