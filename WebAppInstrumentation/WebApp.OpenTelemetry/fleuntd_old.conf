<system>
  log_level info
</system>

<source>
  @type http
  port 8888
  bind 0.0.0.0
  add_http_headers false
</source>

<match **>
  @type copy

  <store>
    @type file
    path ./logs/app

    <buffer time>
      @type file
      path ./buffer/file

      timekey 1d
      timekey_wait 10m
      timekey_use_utc true
    </buffer>

    compress gzip
  </store>

<store>
    @type stdout
  </store>
</match>