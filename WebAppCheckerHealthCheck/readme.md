## NOTE:

Comand for get display custom metrics.

    dotnet-counters monitor -n WebAppChecker --counters App.Metric
    dotnet-counters monitor -n WebAppChecker --counters Microsoft.AspNetCore.Hosting
